# 開発環境用の環境変数

# APIのベースURL
# モックサーバーを使う場合: http://localhost:4010
# 実サーバーを使う場合: 空文字列（Vite proxyが /api/* を処理）

# モックサーバーを使う（バックエンド未起動時）
# VITE_API_BASE_URL=http://localhost:4010

# 実サーバーを使う場合は以下のように空にする
# VITE_API_BASE_URL=
